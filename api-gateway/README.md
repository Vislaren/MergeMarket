# API Gateway — Kong Community Edition

Single entry point for all client requests. Handles rate limiting, JWT
validation, and routing to backend services. Configured declaratively.

- **Files:** `kong.yml` (declarative config), `plugins/` (custom plugin config)
- **Task:** A-09

_Configuration pending._
