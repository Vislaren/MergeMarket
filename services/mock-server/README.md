# Mock Server (Go) — local dev only, not deployed

Serves the API contracts in `project_docs/api/API_CONTRACTS.md` with static
fixtures so Agent B can develop the Flutter app offline without the full
backend running.

- **Endpoints:** mirrors all `/api/v1/*` contracts + `GET /health`
- **Task:** (not yet scheduled in Agent A TODO)

_Implementation pending._
